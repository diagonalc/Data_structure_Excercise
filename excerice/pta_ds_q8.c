#include <stdio.h>
#include <stdlib.h>
#define MAX_NODE 30

typedef struct bnode
{
    int val;
    struct bnode* left;
    struct bnode* right;
} node;


int pre[MAX_NODE] = {0};
int in[MAX_NODE] = {0};
int post[MAX_NODE] = {0};
int n;

int build_tree()
{
    
}

int main()
{
//if n == 1
    return 0;
}