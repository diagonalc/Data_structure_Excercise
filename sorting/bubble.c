#include <stdio.h>
#include <stdlib.h>

void bubble(int *arr, int n)
{
    for
}

int main()
{
    int n;
    int arr[n];
    scanf("%d", &n);
    for (int i = 0; i < n; i++)
        scanf(" %d", &arr[i]);
    bubble(arr);

    for (int i = 0; i < n; i++)
        printf("%d ", arr[i]);

    return 0;
}